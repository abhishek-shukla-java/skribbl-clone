# Skribbl Clone Project

This workspace contains the scaffold for a React + Vite frontend and a Node + Express + Socket.IO backend for a Skribbl.io-style game.

## Setup commands

### Frontend
cd frontend
npm install
npm run dev

### Backend
cd backend
npm install
npm run dev
